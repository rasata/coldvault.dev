# Custom YARA rules — coldvault.dev

Drop `.yar` / `.yara` files here. They are auto-loaded by
`scripts/scan-malware.sh` alongside the community + signature-base rulesets.

See `.claude/skills/yara-malware-hunter/SKILL.md` for authoring guidance.
