# Custom Semgrep rules — coldvault.dev

Drop project-specific `.yml` files here. They are auto-loaded by
`scripts/scan-sast.sh` via `--config rules/semgrep`.

See `.claude/skills/semgrep-rule-creator/SKILL.md` for authoring guidance.
